import React, { Component } from 'react'
import styled from 'styled-components'

import { Actions, Selectors, States } from '@/utils/decorators'

interface Props {
  
}

export default function FolderPanel( {}: Props ) {
  return (
    <StyledRoot>
      FolderPanel
    </StyledRoot>
  )
}

const StyledRoot = styled.div``